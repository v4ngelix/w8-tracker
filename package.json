{
  "name": "w8-tracker-be",
  "version": "1.0.0",
  "description": "BE for the W8 application",
  "main": "app.js",
  "scripts": {
    "start": "node --env-file=./configurations/.env server/application.ts",
    "backup": "git pull && git add weights.db && git commit -m \"Update weights.db\" && git push"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.7.2",
    "typescript": "^5.8.3"
  }
}
